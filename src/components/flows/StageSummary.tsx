import { MarkdownContent } from "./MarkdownContent";

interface StageSummaryProps {
  summary: any;
}

export const StageSummary = ({ summary }: StageSummaryProps) => {
  return null; // Removing stage summary functionality as requested
};